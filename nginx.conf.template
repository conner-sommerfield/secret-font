server {
    listen 80;

    location /SECRET_PLACEHOLDER/ {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location / {
        return 404;
    }
}