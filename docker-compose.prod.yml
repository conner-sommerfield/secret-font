services:
  frontend:
    build:
      context: .
      target: server
    ports:
      - "80:80"
    volumes:
      - ./generated/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/secret-font/secret.key:/run/cat_pass.txt:ro
      - /etc/secret-font/secret.key:/run/zix_pass.txt:ro